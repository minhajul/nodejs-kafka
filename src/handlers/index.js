export async function processOrder(message) {
    console.log('🛒 Handling order:', message);
}

export async function processPayment(message) {
    console.log('💳 Handling payment:', message);
}